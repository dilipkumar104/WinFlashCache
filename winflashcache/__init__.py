"""
WinFlashCache — A lightweight, persistent CLI key-value store.

This package provides a Redis-like key-value store that runs entirely
in your terminal. Data is persisted to disk so it survives restarts.
"""

__version__ = "1.0.0"
__app_name__ = "WinFlashCache"
